import '../../../trainer_dashboard/domain/entities/student.dart' as dashboard;

// Re-export the student entity from dashboard domain
// This maintains the existing structure while allowing for future customization
typedef Student = dashboard.Student;
typedef StudentStatus = dashboard.StudentStatus;
typedef SubscriptionType = dashboard.SubscriptionType;
typedef StudentRoutine = dashboard.StudentRoutine;
typedef Payment = dashboard.Payment;
